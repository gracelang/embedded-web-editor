var a := -7.6
print (a.rounded)       // this rounds a to the nearest integer
print (a.truncated)     // this drops the decimal part of a
var b := "hello"
print (b.at 1)          // this gets the first letter of b
def angle = π/6
print (angle.sin)       // the trigonomentric sine of angle, treated as radians
